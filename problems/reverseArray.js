/** reverseArray
 * 
 * Given an array, return the reversed array
 * 
 * [1,2,3] => [3,2,1]
 * [2,5] => [5,2]
 */

function reverseArray(arr) {
  let reversed = [];
  for (let i = a.length - 1; i >= 0; i--) {
    reversed.push(a[i]);
  }
  return reversed;
}